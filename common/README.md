# Platform Common

This directory is reserved for code shared across platform-specific samples.

Current state:

- the active sample lives under `platform/windows/`
- future platform-specific implementations can be added alongside it
- shared abstractions, test helpers, or cross-platform Rust/C++ code can live here
